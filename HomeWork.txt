Как организовать клиент-серверное взаимодействие?
Как и в каком виде передавать файлы?
Как пересылать большие файлы?
Как пересылать служебные команды?
Что хранить в базе данных?
Как передавать структуру каталогов/файлов?
Какую библиотеку использовать для сетевого взаимодействия: java.io, java.nio, Netty?

1. через сеть с помощью какой то из библиотек Java.io, java.nio, Netty
2. использовать буферизацию и после передачи сравнивать контрольные суммы, для контроля целостности
3. парсить передаваемые данные на предмет команд например

if(command.startsWith("touch ")){
            String errorMsg = "File is already exists";
            Path path = Paths.get(serverPath.toString() + "/" + command.substring(6));
            if(!Files.exists(path)) Files.createFile(path);
            else channel.write(ByteBuffer.wrap(errorMsg.getBytes()));

        }

4. данные для авторизации пользователей, возможно данные о изменениях в хранимых файлах
5. нет ответа в этом я пока не разобрался)
6. судя по последней лекции Netty)


долго морочился с передачей файла через сеть, в итоге все разломал, и решил что все равно этому рано или поздно научат в рамках курса)